# Local experimentation only.
# The hackathon validator injects API_BASE_URL and API_KEY at runtime.

API_BASE_URL=https://router.huggingface.co/v1
API_KEY=your-openai-compatible-key

# Optional fallback if you prefer using the Hugging Face router token locally.
HF_TOKEN=

MODEL_NAME=Qwen/Qwen2.5-72B-Instruct

# Optional runtime configuration
# ENV_BASE_URL=http://localhost:8000
# LOCAL_IMAGE_NAME=support-ops-env:latest
